<div class="css">
    <div class="left">
    <h1><%= @user.name %>のページ</h1>

    <p>メールアドレス: <%= @user.email %></p>
    <p>プロフィール写真: <%= image_tag @user.picture.url, size: "100x100"%></p>
    <% if current_user.id == @user.id %>
        <%= link_to '編集', edit_user_path(@user), class:"link_button" %> |
    <% end %>
    <%= link_to '投稿する', new_feed_path, class:"link_button" %> |
    <%= link_to '投稿一覧へ', feeds_path, class:"link_button" %>
    </div>
</div>


